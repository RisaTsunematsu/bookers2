<% if @book.errors.any? %>
  <%= @book.errors.count %>errors prohibited this obj from being saved:
  <ul>
    <% @book.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
  </ul>
<% end %>

<div class="container">
 <div class="row">
  <div class="col-sm-12 col-md-8 col-lg-5 px-5 px-sm-0 mx-auto">

<h2>Editing Book</h2>

<%= form_with model: @book, local: true do |f| %>

 <div class="form-group">
  <label for="stringTitle">Title</label>
  <%= f.text_field :title, autofocus: true, id:"stringTitle", placeholder:"Title"%>
 </div>
 <div class="form-group">
  <label for="textBody">Opinion</label>
  <%= f.text_field :body, autofocus: true, id:"textBody", placeholder:"Opinion" %>
 </div>
  <%= f.submit "UpdateBook" %>
<% end %>

<%= link_to "Show",book_path %>
|
<%= link_to "Back", books_path %>
  </div>
 </div>
</div>


<footer>
  <div class="container mt-5">
   <div class="row">
     <div class="mx-auto">
       <p>CopyRight Infratop.inc</p>
     </div>
   </div>
  </div>
</footer>